export const environment = {
  production: true,

  /**
   * REQUIRED. Base URL of the client API, e.g. 'https://api.example.com/v1'.
   *
   * Deliberately empty rather than a plausible-looking placeholder: an
   * unreachable stand-in URL produces a build that looks fine and silently
   * returns no clients. Empty fails immediately with a message that names the
   * problem.
   */
  apiBaseUrl: '',

  useMockData: false,
};
