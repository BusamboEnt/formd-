export interface Client {
  id: string;
  name: string;
  address: string;
  city: string;
  postalCode: string;
  email: string;
  phone: string;
  referenceNumber: string;
  companyName?: string;
}
